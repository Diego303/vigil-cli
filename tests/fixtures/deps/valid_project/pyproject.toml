[project]
name = "my-valid-project"
version = "1.0.0"
dependencies = [
    "click>=8.1",
    "pydantic>=2.0",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.4",
]
